[project]
name = "alpaca-options-agent"
version = "0.1.0"
description = "Autonomous options trading agent — Alpaca AI Trading Agents Hackathon 2026"
requires-python = ">=3.11"
dependencies = [
    "alpaca-py>=0.33",
    "python-dotenv>=1.0",
    "pandas>=2.2",
    "openai>=1.40",
]

[tool.uv]
dev-dependencies = ["ruff>=0.6"]
